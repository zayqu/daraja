export const metadata = {
  title: "Post a Job",
  description:
    "Submit a job opportunity for review and reach job seekers across Tanzania.",
  alternates: {
    canonical: "/post-job",
  },
};

export default function PostJobLayout({ children }) {
  return children;
}
