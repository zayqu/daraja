name: Daraja Job Scrapers

on:
  schedule:
    - cron: "17 * * * *"
  push:
    branches:
      - master
    paths:
      - "scraper/**"
      - ".github/workflows/job-source-scrapers.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: job-source-scrapers-v3
  cancel-in-progress: true

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      DATABASE_URL: ${{ secrets.DATABASE_URL }}
      RESEND_API_KEY: ${{ secrets.RESEND_API_KEY }}
      JOB_ALERTS_FROM_EMAIL: ${{ vars.JOB_ALERTS_FROM_EMAIL }}
      SCRAPER_HEALTH_REPORT: scraper-health.json
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "22"
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Chromium for Ajira rendered listings
        run: npx playwright install --with-deps chromium

      - name: Generate Prisma client
        run: npx prisma generate

      - name: Validate database configuration
        run: |
          if [ -z "$DATABASE_URL" ]; then
            echo "::error::Repository secret DATABASE_URL is not configured."
            exit 1
          fi

      - name: Run enabled source scrapers
        run: npm run scrape

      - name: Preserve scraper health report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-health-${{ github.run_id }}
          path: scraper-health.json
          if-no-files-found: warn
          retention-days: 30
